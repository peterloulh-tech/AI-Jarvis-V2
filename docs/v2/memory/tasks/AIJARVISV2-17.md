# AIJARVISV2-17 — 形成 V1 源码复用分级矩阵与提取边界

- 状态：`done`（Dashi 于 2026-08-07 回读）
- 交付提交：`559c906ab25e3ac1a5f4636f720e3a15d80297ce`
- 直接依赖：[16](AIJARVISV2-16.md)

## 最终结果与关键文件

新增[V1 源码复用分级矩阵](../../requirements/v1-source-reuse-matrix.md)，对固定提交 `dd8fbf9` 的 21 个源码边界逐项记录等级、理由、依赖/许可、提取前置条件和缺口。结论为 `D/M/R/W = 2/13/4/2`：纯 CRC 与部分音频工具可按符号直接复用；采集、IPC、runtime、Overlay、进程及打包需修改；调度、生命周期、发送只参考；V2 编排和非 V2 产品域重写。

## 后续不得破坏的约束

`D` 级也只能从固定提交按符号提取并补测试；`M` 级须先冻结 V2 契约；`R/W` 禁止复制实现。vendor、模型权重和媒体资产不在本任务白名单，许可结论仍由任务 18～20 收口。

## 验证与遗留

点名领域 10/10、矩阵字段 21/21、四级结论 4/4 已静态复核，业务源码/配置/测试/vendor 相对固定提交 0 差异。真实缺口为临时原始音画落盘、采集源不完整、O/V 调度与代次不匹配、语义去重冲突、Overlay/安装生命周期不足及第三方许可未收口；本任务未运行源码或测试。

- 检索关键词：dd8fbf9，D/M/R/W，提取白名单，CRC，DXGI，WASAPI，named pipe，runtime adapter，重写缺口
