# AIJARVISV2-94 — 项目基础规则文档梳理

- 状态：`in_review`（2026-08-19）
- 交付提交：`44eb4fb`、`d9fbedf`、`0ce6a5c`
- 直接依赖：无

## 最终结果与关键文件

按个人创意项目的快速开发和低 Token 目标，压缩 `AGENTS.md`、memory 入口和需求索引；保留四方审计、Official-First、V/O 独立、冻结需求及证据权威。当前状态只保留事实，任务卡只在评审/完成阶段维护。

## 后续不得破坏的约束

不得修改正式 FR/NFR/验收基线、`DECISIONS.md`、官方基线、许可证边界或历史证据；不得复制三文鱼业务规则；默认本地 commit，未经明确要求不 push。

## 验证与遗留

已检查入口结构、相对链接和文档职责。历史任务资料仍按需读取；详细需求正文不在本任务合并。基线哈希以 `docs/v2/requirements/baseline-manifest.md` 为当前权威，旧任务记录中的哈希保留其历史属性。

- 检索关键词：文档索引、最小读取、四方审计、Official-First、低 Token、本地优先
