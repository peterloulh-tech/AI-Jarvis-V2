# AI Jarvis V2 P1 性能与 4/8 小时资源记录模板

> 任务：AIJARVISV2-3
>
> 原始样本 CSV：[templates/performance-sample-record.csv](templates/performance-sample-record.csv)
>
> 资源趋势 CSV：[templates/resource-trend-record.csv](templates/resource-trend-record.csv)

模板中的空白字段应由具体执行任务填写。填写数据不会自动生成冻结门槛；结论只能使用 `PASS_FROZEN`、`FAIL_FROZEN`、`DATA_COMPLETE_PENDING_CALIBRATION`、`DATA_INCOMPLETE` 或 `REVIEW_REQUIRED`。

## 1. 测试运行登记

| 字段 | 记录值 |
|---|---|
| scenario_run_id / 场景 ID | |
| 测试日期 / 执行人 | |
| 应用提交 / 构建 / 配置快照哈希 | |
| Windows 版本 / NVIDIA 驱动 | |
| GPU / 显存 / CPU / RAM / 电源策略 | |
| 模式 / 模型 / 模型版本 / 量化 / 运行时 | |
| V 并发 / 图数，或 O 档位 / 预算 / 节奏 | |
| 回放或输入资产哈希 / 金标版本 | |
| 预热规则 / 样本纳入规则 | |
| 资源采样工具 / 采样间隔 / 进程归属规则 | |
| 测量窗口 / 中断或配置变化 | |
| 原始记录 / 日志 / 图表证据路径 | |

## 2. P1 性能数据汇总

每一行只对应一个 `sample_group`；不得合并不同硬件、模型、并发、图数、档位或输入。

| sample_group | 场景 | MET ID | N 总计 | N 成功 | N 失败 | N 超时 | N 取消 | N 过期 | N 降级 | min | max | P50 | P95 | 吞吐 | 显存 P50/P95/max | GPU P50/P95/max | 证据 | 结果标记 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|---|---|---|
| | | | | | | | | | | | | | | | | | | |

所有 outcome 比率均以该行 `N 总计` 为共同分母；失败、超时、取消、过期和降级不得合并或互相覆盖。

### AC-V10 数据完备性检查

- [ ] V 8GB 与 V 12GB 分开记录，不把产品边界写成性能结论。
- [ ] 1、2、3 路并发分别记录延迟、吞吐、显存和 GPU。
- [ ] 每组记录模型/量化/运行时、共享权重实例数和失败分母。
- [ ] 1、2、3 张画面如参与测试，按图数独立分组。
- [ ] 最终数值门槛保持待实测/待评审。

### AC-O10/第 23.2 节数据完备性检查

- [ ] O 16GB 仅按模型独占基线记录，不声明单机高负载游戏能力。
- [ ] 低/标准/高分别保留冻结条数与 12/8/6 秒节奏，报告首输出、完整结果、P50/P95。
- [ ] 自定义 225/350/500 字预算记录冻结 12/16/20 秒硬超时判定，不改值。
- [ ] 高档记录持续忙碌比例和单待生成槽覆盖/丢弃。
- [ ] 持续音画、自主触发、结构化文字的可行性仍属于选型证据，不由本模板宣布通过。

## 3. 4/8 小时资源趋势汇总

| 字段 | 4 小时正式窗口 | 8 小时内部窗口 |
|---|---|---|
| scenario_run_id / 模式 / 固定输入 | | |
| 计划时长 / 有效时长 / 中断原因 | 4h / | 8h / |
| 主程序崩溃数 | 目标为冻结值 0；实际： | 内部记录： |
| 显存初值 / P50 / P95 / max / 末值 / 分段斜率 | | |
| 主机内存初值 / P50 / P95 / max / 末值 / 分段斜率 | | |
| 句柄初值 / P50 / P95 / max / 末值 / 分段斜率 | | |
| GPU P50 / P95 / max / busy 比例 | | |
| 各队列/缓冲上限 / 峰值 / 触顶 / 淘汰 | | |
| 日志与历史初值 / max / 末值 / 滚动次数 / 斜率 | | |
| 模型重启 / 会话重建 / 全局复位 / 恢复失败 | | |
| 过期 / 格式失败 / 降级 / 其他失败 | | |
| 停止后显存 / 剩余 PID / 孤儿进程 | | |
| 趋势图与原始记录证据 | | |
| 结果标记 | | |

资源斜率用于展示趋势，不得在没有冻结门槛时自行把某个 MiB/h 或句柄/h 数值定义为最终通过线。若出现持续单向增长、无法解释的阶跃或记录不足，使用 `REVIEW_REQUIRED` 或 `DATA_INCOMPLETE`，交后续评审。

## 4. 端到端阶段分解

| sample_group | observed→captured | captured→submitted | submitted→inference started | inference started→result completed | result completed→validated | validated→enqueued | enqueued→send started | send started→first displayed | observed→first displayed | 过期/淘汰/失败分母 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| | | | | | | | | | | |

必须同时报告 O/V、普通/高光/高光降级和空闲/拥塞路径；缺失任何关键关联点时，该样本标为无效并保留缺失原因。

## 5. 评审结论与承诺边界

| 项目 | 记录 |
|---|---|
| 已按冻结要求判定的项目 | |
| 数据已完成但门槛待校准的项目 | |
| 失败或证据不完整的项目 | |
| 需要评审的新门槛/冲突 | |
| 对外可引用的冻结原文 | |
| 明确不得对外承诺的实测结果 | |

评审不得仅凭单次最佳值、平均值或一张趋势图确定门槛。任何新增性能承诺必须引用原始样本、统一统计、测试环境和正式评审记录，并由后续负责门槛冻结的任务处理。
