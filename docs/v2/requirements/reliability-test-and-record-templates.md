# AI Jarvis V2 可靠性、故障恢复与长稳记录模板

> 任务：AIJARVISV2-4
>
> 故障事件 CSV：[templates/reliability-event-record.csv](templates/reliability-event-record.csv)
>
> 任务3资源趋势 CSV：[templates/resource-trend-record.csv](templates/resource-trend-record.csv)
>
> 任务3性能样本 CSV：[templates/performance-sample-record.csv](templates/performance-sample-record.csv)

本模板记录可靠性行为和判定。性能延迟、资源时序、P50/P95 和端到端数据继续写入任务3模板；本文件只引用证据，不复制或改变其字段口径。

## 1. 故障注入运行登记

| 字段 | 记录值 |
|---|---|
| reliability_run_id / FAULT ID | |
| 日期 / 执行人 / 应用提交 / 构建 | |
| Windows / 驱动 / 硬件概要引用 | |
| 模式 / 模型 / 量化 / 运行时 | |
| run_id / 注入前 run_generation | |
| sample_group（O/V、故障类型、模型/运行时/硬件分组键） | |
| 只读配置快照哈希 / 固定输入哈希 | |
| 故障组件 / 类型 / 注入方法 / 注入时点 | |
| 期望信号 / 动作 / 最大残留 | |
| 实际信号 / 动作 / 新代次 | |
| 故障前后 PID / 模型与权重实例 | |
| 队列、缓冲、重试上限与峰值 | |
| 停止前后显存 / 剩余 PID / 孤儿 | |
| 事件 CSV / 性能 CSV / 资源 CSV / 日志证据 | |
| 结果标记 / 评审说明 | |

## 2. 单次故障闭环

| 阶段 | 必须记录 | 通过条件 |
|---|---|---|
| 故障前 | 组件 PID、状态、代次、在途任务、队列与配置上限 | 环境与控制变量齐全 |
| 故障确认 | MP-19、显式信号、错误码、首次确认时间 | 只使用冻结已有信号 |
| 动作开始 | MP-20、动作、attempt_index、受影响组件 | 与故障矩阵一致，无未登记动作 |
| 隔离观察 | UI 控制、其他组件、旧代次结果、队列变化 | 主程序可操作，故障不扩散，旧结果为 0 |
| 恢复就绪 | MP-21、新代次、实际结果、失败原因 | 冻结恢复行为满足；耗时只记录 |
| 停止清理 | MP-03/04/23、显存、PID、临时状态 | 孤儿 0，模型资源完成清理 |

## 3. 暂停、恢复与停止检查

| 检查项 | 空闲 | 模型忙碌 | 队列/发送拥塞 | 故障中 |
|---|---|---|---|---|
| 暂停后 MP-04 存在 | | | | |
| 暂停后新增分析任务数为 0 | | | | |
| 暂停后新增发送数为 0 | | | | |
| 队列、上下文和摘要清空 | | | | |
| 在途/旧代次结果生效数为 0 | | | | |
| 暂停期间配置继续锁定 | | | | |
| 恢复创建新代次 | | | | |
| 恢复只使用当前新画面 | | | | |
| 停止后配置解锁 | | | | |
| 停止后模型显存和进程清理 | | | | |

## 4. 4 小时正式稳定性验收

O/V 必须分开执行，使用固定输入或稳定画面源，正式窗口不注入异常。资源原始数据填写任务3 `resource-trend-record.csv`，本节只记录可靠性出口。

| 项目 | 冻结期望 | 实际/证据 |
|---|---|---|
| 计划时长 / 有效时长 | 完整 4h | |
| 主程序非预期崩溃数 | 0 | |
| UI 停止/退出操作 | 每次均可接受 | |
| 未响应、白屏、失去控制事件 | 逐次记录；不得掩盖 | |
| 模型/采集/Overlay 非预期退出 | 逐次记录 | |
| 有界结构触顶/淘汰/滚动 | 有上限且动作可解释 | |
| 资源趋势证据 | 引用任务3资源 CSV，不自行设增长门槛 | |
| 停止后显存清理 | 满足冻结清理结果 | |
| 停止后本次孤儿进程数 | 0 | |
| 结果标记 | `PASS_FROZEN` / `FAIL_FROZEN` / `DATA_INCOMPLETE` | |

## 5. 8 小时内部压力与重复故障

8 小时是内部工程目标。各阶段、故障类型和注入次数必须预先登记；恢复次数、增长斜率和保护阈值均不得转成对外承诺。

| 项目 | 记录值 |
|---|---|
| 计划时长 / 有效时长 / 中断原因 | 8h / |
| 阶段时间线 / 固定输入哈希 | |
| O 会话重建 / 进程重启 / 失败 | |
| V 全局复位 / 进程重启 / 失败 | |
| 采集失效 / Overlay 异常 / 控制动作 | |
| 各等待、队列、缓冲、重试上限与峰值 | |
| 过期 / 淘汰 / 格式失败 / 降级 | |
| 显存、内存、句柄、日志和历史趋势证据 | 任务3资源 CSV 引用： |
| 停止清理 / 剩余 PID / 孤儿 | |
| 数据完整性 / 待校准 / 评审事项 | |

## 6. 异常退出与单实例

| 场景 | 期望结果 | 实际/证据 |
|---|---|---|
| 配置状态启动第二实例 | 只唤起已有窗口；第二实例/模型/会话新增数 0 | |
| 运行状态启动第二实例 | 同上，当前运行不被重置 | |
| 暂停状态启动第二实例 | 同上，暂停状态保持 | |
| 恢复/故障处理时启动第二实例 | 同上，不并发加载第二模型 | |
| 主程序异常退出后再启动 | 清理上次遗留 PID 和最小状态，创建新 run_id | |
| 异常退出后的数据边界 | 只允许最小锁标记；无用户内容或原始音画遗留 | |

## 7. 评审结论

| 分类 | 记录 |
|---|---|
| 冻结结果全部满足 | |
| 冻结结果失败 | |
| 数据完整但恢复时间/趋势/阈值待校准 | |
| 数据或证据不完整 | |
| 未登记故障或基线冲突，需要评审 | |
| 明确不得形成的对外承诺 | |

评审不得用平均恢复时间、短窗口资源趋势或单次成功覆盖硬要求失败。任何新增恢复时间、V 超时、重启保护、资源增长或性能门槛必须由后续实测任务提交证据并正式评审。
